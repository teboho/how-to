"use client";

const Error = (): React.ReactNode => {
    return (
        <section>
            <h1>Error {`:(`}</h1>
            <p>Somehting went wrong</p>
        </section>
    );
}

export default Error;